* 【判断子序列】
* 给定字符串a,b。判断字符串a是否为字符串b的子序列。
* 子序列判断的规则为；不要求字符为连续的，但是字符之间的相对位置不能改变！
* <p>
* 示例 1：
* 输入：s = "abc", t = "ahbgdc"
* 输出：true

思路：

1.只要字符串a的所有字符以正确的相对位置在字符串b中出现的话，就说明是子序列，那我们只需要使用双指针来进行判断即可。

2.leftA 和 leftB ，外层的while循环就是：leftB < lenB ；只要在这个条件中满足，leftA == lenA ，就可以返回true了！


