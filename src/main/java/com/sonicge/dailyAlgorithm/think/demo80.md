* 【分割回文串】
* 给你一个字符串 s，请你将 s 分割成一些 子串，使每个子串都是 回文串 。返回 s 所有可能的分割方案
* 输入：s = "aab"
* 输出：[["a","a","b"],["aa","b"]]

思路：

1.这道题是一个典型的回溯思路，假如我们给定aab，就是在某个单词后面砍一刀。比如"a" "a" "b"

2.如果可以成为回文串的话，就添加到part中，然后对sb重新赋值，交给下一个递归继续判断

3.如果不能成为回文串的话，继续拼接sb，然后继续判断！
