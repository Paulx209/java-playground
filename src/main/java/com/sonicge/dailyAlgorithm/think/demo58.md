* 【二叉树中第k小的元素】
* 给定一个二叉搜索树的根节点 root ，和一个整数 k ，请你设计一个算法查找其中第 k 小的元素（k 从 1 开始计数）。
*
* 输入：root = [5,3,6,2,4,null,null,1], k = 3
* 输出：3

思路：

1.二叉树的一个规律就是，该节点左边的元素都比该节点小，该节点右边的元素都比该节点大

2.所以在二叉树中，中序遍历后，就是按照升序排列的，所以只需要一次遍历，就可以获取到第k小的元素了。

3.又因为k从1开始，所以对应的下标值就是k-1
