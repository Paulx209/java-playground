* 【最小栈】
* 设计一个支持 push ，pop ，top 操作，并能在常数时间内检索到最小元素的栈。
* 实现 MinStack 类:
* int getMin() 获取堆栈中的最小元素。

思路：

1.如何实现常数时间检索到最小元素的栈呢？我使用了一个集合来维护，每次添加新元素的时候，对元素进行重新排序

2.元素出栈的时候，只需要将list中的这个元素删掉即可。

3.获取最小元素的时候，就是list.get(0)
