* 【最长回文子串】
* 给你一个字符串 s，找到 s 中最长的 回文 子串。
* 输入：s = "babad"
* 输出："bab"
* 解释："aba" 同样是符合题意的答案。

思路：

1.从中间向两边进行扩展，直到不符合回文串

2.从哪个点开始呢？如果字符串的长度为奇数的话，从中间点开始即可；如果字符串的长度为偶数的话，从中间两个值开始即可

3.最后在遍历的过程中维护一个res字符串即可！
