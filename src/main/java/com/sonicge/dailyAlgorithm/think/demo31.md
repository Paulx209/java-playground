* 【同构字符串】
* 给定两个字符串 s 和 t ，判断它们是否是同构的。
* 如果 s 中的字符可以按某种映射关系替换得到 t ，那么这两个字符串是同构的。
* 输入：s = "egg", t = "add"
* 输出：true

思路：

1.使用两个哈希表做对应的映射，比如说e->a ; g->d

2.如果map1包含字符e的话，就说明之前这对字符应该映射过，然后就判断map1.get(e) == a ；同时map2中也应该判断

3.如果有不符合这个条件的，直接return false 即可；反之 return true
