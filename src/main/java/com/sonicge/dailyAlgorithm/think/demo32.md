* 【单词规律】
* 给定一种规律 pattern 和一个字符串 s ，判断 s 是否遵循相同的规律。
* 输入: pattern = "abba", s = "dog cat cat dog"
* 输出: true

思路：

1.同样还是使用两个字符串来进行维护，之前我们学习的是字符之间的映射，现在是字符和字符串之间的映射，难度升高了一些

2.但是思路还是一样的， 建立字符和字符串之间的映射即可。
