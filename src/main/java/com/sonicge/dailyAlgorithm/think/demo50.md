* 【旋转链表】
* 给你一个链表的头节点 head ，旋转链表，将链表每个节点向右移动 k 个位置。
* 输入：head = [1,2,3,4,5], k = 2
* 输出：[4,5,1,2,3]

思路：还是比较简单的

1.旋转链表其实就是被一个index分成两部分，第二部分的末尾接上第一部分的开头。

2.我们首先要找到这个index，也就是k%size(因为这里的k不是<size的，所以要对他做取余操作)

3.然后将第二部分的最后一个元素 .next = dummyHead.next即可。
