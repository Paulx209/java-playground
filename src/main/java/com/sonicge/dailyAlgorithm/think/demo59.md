* 【二叉搜索树中的最小绝对差】
* 给你一个二叉搜索树的根节点 root ，返回 树中任意两不同节点值之间的最小差值 。
* 差值是一个正数，其数值等于两值之差的绝对值。
*
* 输入：root = [4,2,6,1,3]
* 输出：1

思路：

1.首先因为他是一个二叉搜索树，所以左子树的值都比节点小，右子树的值都比节点大。

2.那中序遍历之后，就变成一个升序排列的集合了，所以我们可以使用一个pre变量来记录前一个值的大小

3.这样最小绝对差的值一定会在相邻两个数字中出现

所以，中旬遍历 + pre记录前一个值 + 每次遍历都更新min变量


